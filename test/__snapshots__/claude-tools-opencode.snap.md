=== .opencode/agents/dev.md ===
---
description: Helper that's tool-restricted.
---

A tool-restricted dev agent body.

=== README.md ===
# claude-tools — OpenCode bundle

Fixture: claude tools array → CSV join + disallowedTools + copilot description with apostrophe.

## Install

Copy or symlink the `.opencode/` directory into your project root:

```bash
# from this directory
cp -R .opencode /path/to/your/project/
```

Or install globally:

```bash
mkdir -p ~/.config/opencode
cp -R .opencode/* ~/.config/opencode/
```
