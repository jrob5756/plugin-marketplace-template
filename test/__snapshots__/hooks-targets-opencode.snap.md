=== .opencode/agents/hooked.md ===
---
description: An agent in a plugin that ships hooks scoped to claude+copilot only.
---

Hooked agent body.

=== README.md ===
# hooks-targets — OpenCode bundle

Fixture: hooks scoped to specific targets, suppressing OpenCode warning.

## Install

Copy or symlink the `.opencode/` directory into your project root:

```bash
# from this directory
cp -R .opencode /path/to/your/project/
```

Or install globally:

```bash
mkdir -p ~/.config/opencode
cp -R .opencode/* ~/.config/opencode/
```
